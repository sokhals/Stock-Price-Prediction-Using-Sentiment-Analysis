Instructions to run csvToArff.java

1. create a folder in your inputFiles directory
2. add all the csv file to the directory created in step 1
3. after running the code, give "created folder name" as input

Output:
*.arff files will be created in outputFiles directory 